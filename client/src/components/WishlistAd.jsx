import { wishlist } from "../assets";

const WishlistAd = () => (
  <section>
    <div>
      <img src={wishlist} alt="wishlistAd" />
      <div>
        <p>Make a list for</p>
      </div>
    </div>
  </section>
);

export default WishlistAd;
